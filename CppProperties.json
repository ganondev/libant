{
  "projectName": "libant",
  "configurations": [
    {
      "name": "Core Debug",
      "includePath": [
        "${workspaceRoot}\\c\\include", // It doesn't work without both of these for some reason
        "c/include",                    //
        "c/src",
        "py/include",
        "${env.py_include}\\",
        "${env.VCToolsInstallDir}\\include",
        "${env.crt_include}\\"
      ],
      "defines": [
        "LIBANT_DEBUG"
      ],
      "compilerSwitches": "/Tc"
    }
  ]
}