{
  "projectName": "libant",
  "configurations": [
    {
      "name": "Core Debug",
      "includePath": [
        "${workspaceRoot}\\c\\include", // It doesn't work without both of these for some reason
        "c/include",                    //
        "c/src/quadtree",
        "py/include",
        "${env.py_include}\\",
        "${env.VCToolsInstallDir}\\include",
        "${env.VSINSTALLDIR}\\SDK\\ScopeCppSDK\\SDK\\include\\ucrt"
      ],
      "defines": [
        "LIBANT_DEBUG"
      ],
      "compilerSwitches": "/Tc"
    }
  ]
}